// Generated by CoffeeScript 1.9.3
(function() {
  var loader, setFrameSize;

  setFrameSize = function() {
    return $('iframe#main-frame').height($('.content-wrapper').height());
  };

  loader = function(e) {
    if (e === 'hide') {
      return $('.overlay').css('display', 'none');
    } else {
      return $('.overlay').css('display', '');
    }
  };

  $(document).ready(function() {
    $('a.menu').click(function() {
      var treeview;
      loader();
      $('#main-frame').attr('src', $(this).data('href'));
      $('li.treeview').each(function() {
        return $(this).removeClass('active');
      });
      treeview = $(this).parents('li');
      if (treeview) {
        return treeview.addClass('active');
      }
    });
    setFrameSize();
    $(window).resize(function() {
      return setFrameSize();
    });
    return $('iframe#main-frame').load(function() {
      return loader('hide');
    });
  });

}).call(this);
